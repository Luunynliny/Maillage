# Dependencies
node_modules/

# Build products
#
# dist/ is the Vite client bundle, served by server/index.ts in production. Built from source on
# every install; never committed.
dist/

# Local overrides
.env
.env.local

# Test output
coverage/

# macOS
.DS_Store

# Git worktrees used for isolated feature work
.worktrees/
